This directory is for optional custom isolinux branding/splash files.

To add a custom splash screen for the BIOS boot menu:
- Add a PNG file named `splash.png` (640x480 pixels, 16-colour recommended)
- The build script will automatically copy it to the ISO

This is entirely optional — the installer works fine without it.
